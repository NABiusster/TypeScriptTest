import {styled, Typography} from "@mui/material";

const CardTitle = styled(Typography)(() => ({
    textAlign: 'center',
    variant: 'inherit',
    component:'div',
    underline:'none'
}))

export {CardTitle}